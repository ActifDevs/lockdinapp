Checkpoint ID: 2026-08-19_1734
Generated At: 2026-08-19 17:34
Document Type: Changes
Repository Branch: main
Commit SHA: 428d9fd989c42c8129f7da5095ab3dc517343b63
Scope: Material Changes Since Checkpoint 2026-08-17_0832
Status: Repository Snapshot

# Material Changes

This document details the material architectural, code, database, and operational changes integrated into `main` since the previous checkpoint (`2026-08-17_0832`).

---

## Significant Changes

### 1. Phase 3 Multi-Tenancy Architecture Merged to `main`
- **Change**: Integrated the entire Phase 3 multi-tenancy implementation from `phase3-multitenancy` into `main` (Merge Commit `e84a41b4cd1c6a9bf2ce177f2e9638e79bf97c1d`).
- **Previous State**: Phase 3 was complete on feature integration branch `phase3-multitenancy`; `main` predated multi-tenancy user-scoped persistence.
- **Current State**: `main` contains the full caller-scoped multi-tenancy implementation across database schema, API server routes, and frontend state.
- **Reason**: Final release milestone for multi-user isolation, RLS enforcement, and production readiness.
- **Files / Areas Affected**: Entire monorepo (`lib/db`, `artifacts/api-server`, `artifacts/revision-platform`, `lib/api-zod`).
- **Database Impact**: Active enforcement of user-owned tables (`profiles`, `user_subjects`, `tasks`, `topic_progress`, `past_paper_attempts`, `exam_dates`).
- **User-Facing Impact**: Full multi-user login, signup, onboarding persistence, personal syllabus progress, task creation, past paper logging, and exam countdowns.
- **Compatibility Notes**: Fully backward compatible with API contracts.

### 2. Neutral Topic Progress Default Correction
- **Change**: Introduced explicit neutral topic progress defaults (`status = 'not_started'`, `notes = null`) in API topic progress handlers (`810045afe58f6b1fbaa0e85c8020e095a4112e5c`).
- **Previous State**: Unauthenticated or missing topic progress records could fail legacy column fallback assertions in specific API integration specs.
- **Current State**: API server cleanly returns neutral topic progress defaults for unauthenticated or newly viewed topics without mutating database state.
- **Reason**: Fix post-merge regression assertion while preserving caller-owned overlay architecture.
- **Files / Areas Affected**: `artifacts/api-server/src/routes/subjects.ts`, `artifacts/api-server/src/lib/topic-progress.ts`.
- **Database Impact**: None.
- **User-Facing Impact**: Smooth topic rendering for newly enrolled subjects before user makes first topic status edit.
- **Compatibility Notes**: Preserves legacy column test suite expectations.

### 3. Production Deployment & DATABASE_URL Incident Resolution
- **Change**: Deployed `main` to Vercel production target `actif-devs/lockdinapp-web` (`https://lockdinapp-web.vercel.app`) and resolved missing `DATABASE_URL` runtime incident (Report 63, commit `428d9fd989c42c8129f7da5095ab3dc517343b63`).
- **Previous State**: Production API cold-starts threw `Error: DATABASE_URL must be set` due to missing environment variable on Vercel project `lockdinapp-web`.
- **Current State**: Production environment configured with hosted Lockdin Supabase Session Pooler connection string (`hazvcdrcvsxmuwdfiucx`, port 5432). Production API endpoints `/api/healthz`, `/api/healthz/db`, `/api/subjects`, and auth-guarded user endpoints verified 100% operational.
- **Reason**: Production environment provisioning and incident closeout.
- **Files / Areas Affected**: Vercel deployment configuration & ops documentation (`docs/cursor/reports/63-production-database-url-incident.md`).
- **Database Impact**: None (no source code, migration, or schema changes required).
- **User-Facing Impact**: Full production availability on `https://lockdinapp-web.vercel.app`.
- **Compatibility Notes**: Resolved cold-start serverless boot failure without application code modifications.

---

## Database Changes

- **Migrations Applied**: `0000_syllabus_reference_and_paper_attempts.sql` through `0009_dear_mathemanic.sql` present in `lib/db/migrations/`.
- **Migration `0010`**: Confirmed **ABSENT**.
- **RLS Policy Enforcement**: All 6 user-owned tables (`profiles`, `user_subjects`, `tasks`, `topic_progress`, `past_paper_attempts`, `exam_dates`) enforce `auth.uid() = user_id`.
- **Shared Reference Isolation**: Shared tables (`subjects`, `syllabus_versions`, `syllabus_units`, `syllabus_topics`, `syllabus_learning_outcomes`, `assessment_components`, `learning_outcome_components`) are read-only to client connections and free of user progress state.

---

## Data Pipeline Changes

- Validated CLI syllabus importer (`pnpm --filter scripts import-syllabi`) on `main`.
- Importer manifest (`scripts/src/syllabus/manifest.ts`) configured for all 9 Cambridge AS & A-Level specifications: 9231, 9489, 9609, 9618, 9700, 9701, 9702, 9708, 9709.
- Verified idempotent upsert behavior with ON CONFLICT resolution for units, topics, learning outcomes, and assessment components.

---

## Frontend Changes

- Full integration of authenticated user context across frontend pages:
  - **Dashboard**: Subject mastery grid rendering caller's enrolled subjects; score trend line chart displaying caller's past-paper scores; exam date countdown widgets.
  - **Progress Page**: Topic tree displaying caller's topic completion status and notes.
  - **Past Papers Page**: Practice paper logging interface submitting caller's atomized paper scores to `/api/past-paper-attempts`.
  - **Tasks Page**: User-scoped task creation, filter, completion toggle, and deletion.

---

## Removed / Replaced / Legacy Behaviour

1. **Legacy Syllabus Columns Removed**: `status` and `notes` columns were removed from `syllabus_topics` in Migration `0007`. Topic progress is stored exclusively in caller-scoped `topic_progress`.
2. **Local-Storage User State Replaced**: Prototyping local-storage mocks replaced with database-backed Supabase Auth and RLS persistence.
3. **Legacy Vercel Domain Reclassified**: `https://lockedin-study.vercel.app` reclassified as a legacy teammate-owned preview deployment; canonical production target established at `https://lockdinapp-web.vercel.app`.
