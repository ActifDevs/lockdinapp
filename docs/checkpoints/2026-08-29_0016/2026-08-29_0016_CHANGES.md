Checkpoint ID: 2026-08-29_0016
Generated At: 2026-08-29 00:16
Document Type: Changes
Repository Branch: main
Commit SHA: 0d2f1f429894ccb4db48e5336d8f67346d0c81c6
Scope: Material delta from checkpoint 2026-08-24_1705 at f76fc2191af65c067c19178e93407e84f222870e
Status: Repository Snapshot

# Material Changes Since Previous Checkpoint

The previous checkpoint captured Phase 4 complete and Phase 5 pending. The current canonical repository closes Phase 5 after four frontend slices plus two final-cutover product fixes. No schema migration or syllabus CSV changed between the checkpoints.

## Significant Changes

### 1. Account-scoped notification preferences

```text
Change:              Notification preferences and reminder dedupe state are scoped to the authenticated user ID.
Previous State:      Preference state used an unqualified browser-local key and could be ambiguous across accounts in one browser profile.
Current State:       Keys use <base>:<authenticated-user-id>; the legacy global preference key is discarded and signed-out/loading states use safe defaults.
Reason:              Prevent cross-account browser-local preference leakage during account switches.
Files / Areas:       use-notification-prefs.ts, user-scoped-storage.ts, reminder-runner.tsx, focused tests.
Database Impact:     None. Server persistence was explicitly deferred.
User-Facing Impact:  Each account retains independent preferences in the same browser profile.
Compatibility Notes: Legacy unscoped values are intentionally not attributed to any account.
```

### 2. Read, error, empty, stale, and retry state reconciliation

```text
Change:              Phase 5 target pages now distinguish initial loading, genuine empty results, primary/secondary failures, and stale cached data.
Previous State:      Some failed reads could be presented as empty content or could remove useful cached content without localized recovery.
Current State:       Subject Detail, Study Plan, Past Papers, and Settings catalogue use localized notices, retry controls, stale-cache warnings, and dependency-specific failure boundaries.
Reason:              Prevent false-empty UI and preserve safe usable state during transient failures.
Files / Areas:       read-state-notice.tsx, query-error-message.ts, four target pages, read-state tests.
Database Impact:     None.
User-Facing Impact:  Failures are explicit and recoverable; cached content remains visible where safe.
Compatibility Notes: The global authenticated 401/logout handler remains authoritative; cancellations are not surfaced as errors.
```

### 3. URL-owned navigation state and duplicate-history fix

```text
Change:              Selected tabs, views, filters, and calendar context moved to canonical URL query parameters, followed by an idempotency guard for controlled tab callbacks.
Previous State:      Page-local state reset on refresh/back/forward; Radix automatic activation could produce adjacent duplicate history destinations before controlled state committed.
Current State:       Settings, Subject Detail, Study Plan, Past Papers, and Calendar preserve state through refresh and history. Deliberate changes use PUSH, canonical normalization uses REPLACE, and duplicate pending destinations are suppressed.
Reason:              Make navigation restorable and browser-history-correct while retaining automatic keyboard activation.
Files / Areas:       navigation-query-state.ts, use-idempotent-controlled-navigation.ts, target pages, navigation/history tests.
Database Impact:     None.
User-Facing Impact:  Back/Forward and refresh preserve context without duplicate history entries.
Compatibility Notes: Defaults are omitted from URLs; invalid/duplicate parameters normalize safely. URLs confer no authorization.
```

### 4. Mutation feedback and cache convergence

```text
Change:              Mutations gained localized safe errors and domain-scoped cache reconciliation.
Previous State:      Some dialog/row failures lacked stable inline feedback and cache updates could be broader or inconsistent.
Current State:       Add Task and Log Paper dialogs retain inline failure feedback; task/topic/past-paper deletion errors remain local; bulk topic updates handle partial failure; successful mutations invalidate only affected task, dashboard, progress, syllabus, subject-performance, or attempt keys.
Reason:              Keep visible state convergent with server state without broad cache purges or silent failures.
Files / Areas:       dashboard.tsx, study-plan.tsx, subject-detail.tsx, past-papers.tsx, settings.tsx, mutation tests.
Database Impact:     None.
User-Facing Impact:  Failed actions are recoverable in place and successful actions update related views consistently.
Compatibility Notes: Existing API contracts and auth boundaries are unchanged.
```

### 5. Serverless-safe database pooling

```text
Change:              Database configuration now validates serverless Supabase pooler mode and applies bounded pool/query timeouts.
Previous State:      Vercel runtime used Supabase session pooling on port 5432; multiple warm isolates could retain session connections and authenticated fan-out showed 50-155 second stalls.
Current State:       Serverless Supabase session-pooler URLs are rejected; runtime requires transaction pooling on port 6543. Pool configuration uses max 1, 5s idle, 10s connect, 15s query/statement timeouts, and allowExitOnIdle.
Reason:              Prevent session-pool capacity pressure and prolonged authenticated pending states in serverless production.
Files / Areas:       lib/db/src/index.ts, db-config.test.ts, .env.example, docs/supabase-local-setup.md.
Database Impact:     No schema/data change. Production DATABASE_URL mode changed operationally; secret value was not committed or recorded.
User-Facing Impact:  Original prolonged authenticated pending regression was not reproduced after cutover.
Compatibility Notes: Migration/admin tooling prefers DIRECT_DATABASE_URL and is not moved to transaction pooling.
```

### 6. Phase 5 release evidence and closeout

```text
Change:              Reports 69-82 record entry audit, four slice releases, cutover fixes, regression proof, and final closure.
Previous State:      Phase 5 was pending in the previous checkpoint.
Current State:       Phase 5 is closed with no open Phase 5 blocking defect. P5-CUTOVER-01 and P5-CUTOVER-02 are fixed and production verified; P5-CUTOVER-03 was a stale QA baseline, not a product defect.
Reason:              Preserve implementation, QA, deployment, security, and data-safety traceability.
Files / Areas:       docs/cursor/reports/69-82.
Database Impact:     None.
User-Facing Impact:  Confirms release readiness and production verification of cumulative Phase 5 behavior.
Compatibility Notes: Detailed reports remain historical evidence; this checkpoint summarizes current code rather than replacing them.
```

## Database Changes

- New migrations: none.
- Migration journal: unchanged at `0000` through `0009`.
- Tables, columns, indexes, constraints, RLS policies, grants, and database functions: unchanged between checkpoint commits.
- Runtime connection configuration changed in `lib/db/src/index.ts`; this is operational database-client behavior, not DDL.
- `.env.example` now explicitly separates transaction-pooled serverless `DATABASE_URL` from direct/session-compatible `DIRECT_DATABASE_URL` migration usage.

## Data Pipeline Changes

- Nine syllabus CSV files: unchanged.
- `_audit_report.json`: unchanged.
- Manifest, parser, normalizer, importer, and pipeline tests: unchanged.
- No syllabus/reference data was re-imported by this checkpoint task.
- Documentation correction: the actual scripts are `pnpm --filter @workspace/scripts syllabus:validate` and `syllabus:import`; optional filtering is `--files=`, not the stale `import-syllabi`/`--subject` examples in the prior checkpoint.

## Frontend Changes

- Added reusable read-state and navigation-state utilities.
- Increased coverage from the prior checkpoint's recorded 88 frontend tests/19 files to 208 tests/32 files at this checkpoint.
- Added account-scoped notification preference behavior and legacy-key cleanup.
- Added Phase 5 read-state handling on Subject Detail, Study Plan, Past Papers, and Settings.
- Added URL persistence for Settings tab, Subject Detail tab, Study Plan filter/view, Past Papers filters, and Calendar date/month context.
- Added mutation feedback and targeted cache invalidation across dashboard, tasks, topics, past papers, settings/profile, and subject selection.
- Added controlled-navigation duplicate suppression after final-cutover browser history regression.
- No visual redesign, route expansion, or database-backed preference persistence was introduced.

## Testing and Deployment Changes

- Current API suite: 119/119 passing versus 107 tests recorded previously.
- Current frontend suite: 208/208 passing versus 88 tests recorded previously.
- Typechecks and both production builds pass through pinned native tool entry points.
- Final release reports record successful preview/production verification for each Phase 5 slice and both product fixes.
- At this checkpoint, the canonical site independently returned application health 200, DB health 200, anonymous protected-route 401, and landing page 200.

## Removed / Replaced / Legacy Behaviour

| Earlier behavior | Current disposition |
| --- | --- |
| Unqualified notification preference key | LEGACY / OBSOLETE. Removed and never attributed to a user. |
| Page-local-only tab/filter/calendar state | REPLACED by canonical URL query state on Phase 5 target pages. |
| Duplicate controlled tab history pushes | REPLACED by synchronous pending-destination suppression. |
| Failed read presented as empty content | REPLACED by explicit error/stale/retry states. |
| Broad or incomplete mutation cache handling | REPLACED by domain-scoped invalidation and localized failure state. |
| Supabase session pooler on port 5432 for Vercel runtime | LEGACY / REJECTED in serverless runtime; transaction pooler port 6543 is required. |
| Legacy `syllabus_topics.status` and `.notes` references | Still obsolete from Phase 3; current queries select reference columns only. |
| `artifacts/mockup-sandbox` | Remains non-production component-preview tooling; do not treat it as the deployed app. |

## Deferred Items Confirmed by Current State

- Onboarding draft persistence: DEFERRED.
- Mounted exam-date create/delete UI: DEFERRED, although API endpoints exist.
- Broader editing of existing task fields: DEFERRED.
- Server-persisted/cross-device notification preferences: DEFERRED by Phase 5 design decision.
- External calendar synchronization: NOT IMPLEMENTED.
