Checkpoint ID: 2026-07-29_0156
Generated At: 2026-07-29 01:56 UTC
Document Type: Current State
Repository Branch: main
Commit SHA: 3af194a70b63a5aefccd1229a86e818ebb380238
Scope: Phase 0 environment truth — verified live Supabase schema + syllabus import + API reads
Status: Verified against live database (not code-only)

# Project Snapshot

**Checkpoint ID:** 2026-07-29_0156  
**Date:** 2026-07-29  
**Time:** 01:56 UTC  
**Branch:** main  
**Commit:** `3af194a70b63a5aefccd1229a86e818ebb380238`  
**Development Phase:** Phase 0 complete — environment truth verified  
**Database Status:** Live Supabase Postgres; target schema present; Drizzle journal stamped for `0000`  
**Authentication Status:** Still mock localStorage only (unchanged)  
**Syllabus Data Status:** All 9 subjects imported into live DB; row counts verified  
**Importer Status:** IMPLEMENTED and **executed twice** against live DB — counts identical (idempotent)  
**Core Application Status:** Local API (`PORT=3001`) returns DB-backed `/api/subjects` (9 subjects)  
**Testing Status:** Pipeline unit tests exist; Phase 0 added live migrate/import/API verification  
**Deployment Status:** Frontend Vercel config present; API verified locally against Supabase  
**Known Blockers:** No real auth; no multi-tenancy; no RLS; direct DB host is IPv6-only from this network (session pooler used for Node clients)

---

## What was verified in Phase 0 (evidence)

### Schema / migration

- Fresh Supabase project had **zero** `public` tables before Phase 0.
- `pnpm migrate` from this machine against the **direct** host failed:

  ```text
  getaddrinfo ENOTFOUND db.hazvcdrcvsxmuwdfiucx.supabase.co
  ```

  DNS for the direct host returned **AAAA only** (no A record). No `push` was used.
- Target schema was applied **by hand** in the Supabase SQL editor (bootstrap CREATE for current `lib/db/src/schema/*`), then Drizzle journal stamped:

  - schema `drizzle`, table `__drizzle_migrations`
  - hash `9718f65706db89d53484093be10221f9483e8dfa627b2fd10ed432c59b95cb80`
  - `created_at` `1785172719598` (from `migrations/meta/_journal.json`)
- Important: committed `0000_syllabus_reference_and_paper_attempts.sql` is an **incremental** migration (ALTER/rename assuming a prior `push` schema). On an empty DB it cannot be applied as-is; the hand bootstrap created the **target** shape instead, then stamped `0000` as applied.
- **Phase 0 decision:** this hand-bootstrap + journal stamp **counts as “migrate succeeded”** for the environment-truth definition of done (same end state as a successful `pnpm migrate` on a DB that already had the pre-`0000` push schema). No `drizzle-kit push` was used.

### Connection strings

| Use | Host / port | Notes |
|-----|-------------|--------|
| Node app + import (this machine) | `*.pooler.supabase.com:5432` (Session) | Works over IPv4 |
| Direct `db.<ref>.supabase.co:5432` | IPv6-only AAAA here | `ENOTFOUND` / unusable without IPv4 add-on or IPv6 path |
| Transaction pooler `:6543` | Do not use for DDL / migrate | |

Env var name remains a single **`DATABASE_URL`** (`drizzle.config.ts`, `lib/db/src/index.ts`, syllabus scripts).

### Migration workflow decision (durable)

**Use `pnpm migrate` (tracked SQL + journal) for shared/staging/prod.**  
`drizzle-kit push` is only for throwaway local DBs. Do not `push` against Supabase once migration history exists.

### Syllabus validate

```text
pnpm --filter @workspace/scripts syllabus:validate
```

Overall: OK — 9/9 CSVs, 0 warnings.

### Syllabus import (twice)

```text
pnpm --filter @workspace/scripts syllabus:import
```

**DB counts after run 1 and run 2 (identical):**

| Code | Subject | Units | Topics | Outcomes | Components | Relationships |
|------|---------|------:|-------:|---------:|-----------:|--------------:|
| 9231 | Further Mathematics | 4 | 24 | 84 | 7 | 142 |
| 9489 | History | 21 | 81 | 659 | 4 | 659 |
| 9609 | Business | 5 | 34 | 345 | 4 | 690 |
| 9618 | Computer Science | 19 | 45 | 366 | 4 | 366 |
| 9700 | Biology | 22 | 62 | 405 | 8 | 900 |
| 9701 | Chemistry | 25 | 104 | 554 | 5 | 798 |
| 9702 | Physics | 27 | 81 | 373 | 5 | 518 |
| 9708 | Economics | 7 | 51 | 259 | 4 | 518 |
| 9709 | Mathematics | 6 | 38 | 153 | 9 | 226 |
| **Total** | **9 subjects** | **136** | **520** | **3198** | **50** | **4817** |

Run 2 CLI showed `subject:existing`, `version:unchanged`, create counts `+0` for units/topics/outcomes/components; relationship create counters still print per-run recomputes but **table row counts did not change**.

### API curls (local server on `:3001`)

Mounted under `/api` (`express-app.ts`).

`GET /healthz` → **404** `Cannot GET /healthz`  
`GET /api/healthz` → **200** `{"status":"ok"}`  

`GET /api/subjects` → **200**, 9 subjects, DB-backed `topicsTotal` matching import (24, 81, 34, 45, 62, 104, 81, 51, 38).

---

## Stale / wrong claims in prior checkpoints

### `2026-07-27_1835` (especially wrong vs HEAD even before Phase 0)

| Claim | Reality now |
|-------|-------------|
| Importer Status: **NOT IMPLEMENTED** — no CSV import pipeline | Pipeline exists under `scripts/src/syllabus/` and has been run against live DB |
| Data import: NOT IMPLEMENTED | Import executed; 9 subjects live |
| Database deployment: schema defined; no migrations; no connection verified | Live Supabase; schema present; journal stamped |
| Core app: using mock data only | `/api/subjects` returns live rows from Supabase |
| Architecture: schema defined but not deployed | Deployed (hand bootstrap + journal) |

### `2026-07-28_2156` (closer, but still unverified / outdated on deployment)

| Claim | Reality now |
|-------|-------------|
| Database deployment: **NOT IMPLEMENTED**; no production database configured | Supabase project `hazvcdrcvsxmuwdfiucx` has the schema + reference data |
| Known blocker: “database not deployed to production” | DB is live; remaining blockers are auth / multi-tenancy / RLS |
| Syllabus: “ready for production data load” (not yet loaded) | Data **is** loaded; idempotency proven on live DB |
| Core app: “using mock data; ready for real database integration” | Local API integrated against real DB for subjects list |
| External services: “None currently integrated (no Supabase…)” | Supabase Postgres is in use via `DATABASE_URL` |
| Implies `0000` alone is enough to stand up a fresh DB | On empty DB, `0000` fails (incremental ALTERs); need target bootstrap or prior push schema |

### Doc drift already called out in `docs/lockdin-architecture-plan.md` §0

That plan’s archaeology was correct: the `1835` checkpoint understated the pipeline; this checkpoint supersedes both prior “not deployed / not imported” deployment claims with live verification.

---

## Still true (unchanged)

- Auth is still localStorage mock — no Supabase Auth, no `user_id`, no RLS
- Gamification XP/streaks remain client-side
- No AI assistant / focus-blocker features
- Frontend may still keep `SUBJECT_CATALOG` as offline fallback; do not treat that as the only source now that `/api/subjects` is live

---

## Next phase pointer

Phase 1 exit criteria from the architecture plan are largely met for reference data. Phase 2 starts real auth on `tasks` only (`docs/cursor/02-auth-and-tasks.md`).
