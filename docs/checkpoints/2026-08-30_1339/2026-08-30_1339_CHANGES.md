Checkpoint ID: 2026-08-30_1339
Generated At: 2026-08-30 13:39
Document Type: Changes
Repository Branch: main
Commit SHA: 707e9793506fe5707d1cd836f500b8d4bbc0c990
Scope: Material delta from checkpoint 2026-08-29_0016 at 0d2f1f429894ccb4db48e5336d8f67346d0c81c6
Status: Repository Snapshot

# Material Changes Since Previous Checkpoint

The previous checkpoint captured Phase 5 complete with ten migrations (`0000`-`0009`), one mutable/current reference graph per subject, and an offline CLI cleanup defect. The current repository closes Phase 6 after adding a version lifecycle, immutable import operations, session-aware strict assignment, pin-aware reads/writes, applicability/series policy, a dedicated disposable integration harness, and enforced CI gates. The nine validated CSV files did not change.

## Significant Changes

### 1. Offline syllabus CLI and explicit revision identity

```text
Change:              Validation/dry-run no longer load or close the database importer; real operations require explicit logical revision identity.
Previous State:      Validation normalized all files but could exit 1 without DATABASE_URL; source filename acted as the version natural key.
Current State:       Offline validation exits cleanly; import/adopt/publish require one subject and --revision; filenames are provenance only.
Reason:              Separate safe offline checks from database operations and prevent filenames from becoming revision identity.
Files / Areas:       scripts/src/syllabus/cli.ts, CLI tests, canonical graph hashing.
Database Impact:     None for validation; real operations target lifecycle-aware version rows.
User-Facing Impact:  None directly.
Compatibility Notes: Old broad real-import commands without --revision now fail closed.
```

### 2. Syllabus version lifecycle and immutable graphs

```text
Change:              Syllabus versions gained lifecycle, logical key, content hash, publication timestamps, applicability, and overlap/default constraints.
Previous State:      Version identity was subject + source_file and the importer updated matching graphs in place.
Current State:       New content enters as a draft; published/retired/archived graphs are immutable by importer contract; changed content needs a new key.
Reason:              Preserve historical pins and make future curriculum revisions auditable.
Files / Areas:       migrations 0011-0012, schema/syllabusVersions.ts, db-upsert.ts, adopt.ts, publish.ts.
Database Impact:     New enums, columns, indexes, generated range, btree_gist-backed exclusion, and identity correction.
User-Facing Impact:  Existing users can remain on the graph they originally selected.
Compatibility Notes: All current real hosted graphs were explicitly adopted as `{code}-r001`; no real r002 exists.
```

### 3. Pin preservation and pin-aware reference context

```text
Change:              Retained subject memberships preserve their version pins; reference reads and writes resolve against the caller's pin.
Previous State:      Subject replacement could repin retained rows to the then-current version; reference reads primarily used DEFAULT/current context.
Current State:       Retained rows survive replacement unchanged; members read pinned versions; topics/components/task topics must belong to that pin.
Reason:              Prevent silent curriculum switching and cross-version reference writes.
Files / Areas:       migration 0010, resolve-reference-syllabus-version.ts, pin-reference-writes.ts, affected API routes/tests.
Database Impact:     Replacement RPC changed; no bulk repin/data migration.
User-Facing Impact:  Historical memberships remain stable across new publications/default changes.
Compatibility Notes: Non-members still use DEFAULT for catalogue/reference context.
```

### 4. Structured intended sessions and multi-session UX

```text
Change:              Memberships now record intended exam year/series; onboarding/settings accept a global session plus per-subject overrides.
Previous State:      profiles.exam_session was display text and memberships carried no structured applicability input.
Current State:       user_subjects stores a complete nullable year/series pair; new assignments require a valid structured session; retained-only settings can omit it.
Reason:              Select the correct syllabus family by the student's actual sitting without exposing version controls.
Files / Areas:       migrations 0013/0015, userSubjects schema, OpenAPI/generated clients, onboarding/settings, exam-session utilities/tests.
Database Impact:     New enum and membership columns/checks; overloaded onboarding/replacement RPCs.
User-Facing Impact:  Users can choose one default session and override individual subjects.
Compatibility Notes: Existing rows were preserved; no automatic repin. Feb/Mar remains excluded from product choices.
```

### 5. Applicability and strict server-side assignment

```text
Change:              Published versions have researched applicability windows and per-series product policy; assignment RPCs call a strict resolver.
Previous State:      New memberships selected the one `is_current` version.
Current State:       Resolution requires exactly one published version whose window contains the session and whose policy row is true.
Reason:              Make version assignment deterministic, auditable, and fail-closed as multiple editions become possible.
Files / Areas:       migrations 0011, 0013-0015; applicability manifest/operator; assignment-session API; RPCs.
Database Impact:     New syllabus_version_exam_series table; hosted evidence records 9 windows and 27 policy rows.
User-Facing Impact:  May/June and Oct/Nov choices are shown only when safely resolvable; invalid/out-of-range selections return safe errors.
Compatibility Notes: Feb/Mar policy is false; zero or multiple candidates fail; version IDs never enter client contracts.
```

### 6. Disposable database harness and CI enforcement

```text
Change:              Added a dedicated loopback-only Supabase harness, migration drift checker, HTTP/auth/RLS integration path, lifecycle proofs, and PR jobs.
Previous State:      DB-backed integration needed a manually configured disposable database and was not an enforced authoritative path.
Current State:       CI runs empty-to-head migrations, synthetic r001-to-r002 lifecycle, strict assignment, HTTP/auth/RLS, and cleanup on dedicated ports.
Reason:              Verify migrations and security without Production secrets or hosted writes.
Files / Areas:       scripts/src/db-harness/, fixture Supabase config, API integration harness, .github/workflows/pr-quality.yml.
Database Impact:     Disposable only; current hosted journal remains 0015.
User-Facing Impact:  Indirect release reliability.
Compatibility Notes: Ordinary `lockedinapp` and non-loopback targets fail closed; Docker is required.
```

## Database Changes

| Migration | Material effect |
| --- | --- |
| `0010_preserve_existing_syllabus_version_pins` | Replacement RPC inserts only new memberships and leaves retained pins untouched. |
| `0011_open_sunfire` | Adds lifecycle/applicability enums and fields, ordinal/range machinery, one-DEFAULT and non-overlap constraints. |
| `0012_ordinary_penance` | Corrects identity constraints so logical key is subject-unique while content hash/source filename are not revision identity. |
| `0013_useful_husk` | Adds intended session columns/helpers, initial resolver, and structured onboarding/replacement overloads. |
| `0014_perpetual_nighthawk` | Adds RLS-protected version-series policy table and policy-aware resolver. |
| `0015_silent_sentinel` | Cuts new membership assignment to the strict resolver while preserving retained memberships. |

Migration journal count increased from 10 to 16. Current head is `0015_silent_sentinel`; no `0016` exists. Application schema history remains Drizzle-owned, not `supabase/migrations`.

Hosted closeout evidence records all nine real versions as published `r001`, nine applicability windows, 27 policy rows, no draft or real `r002`, and no membership repin. This checkpoint performed no hosted write or privileged SQL.

## Data Pipeline Changes

- CSV files and `_audit_report.json`: unchanged since the previous checkpoint.
- Offline validation/dry-run database coupling: removed.
- Source filename identity/upsert-in-place model: replaced by logical revision plus graph hash.
- Real import: narrowed to one explicit subject/revision and draft-only creation/rebuild.
- Added explicit legacy adoption and draft publication operations.
- Added version-controlled applicability/policy write-set and validation/apply operator.
- Added canonical hash verification before adoption/publication.
- Added future revision runbook and synthetic disposable `r001` -> `r002` proof.
- Automatic deletion/replacement of published graphs and automatic user repin remain intentionally absent.

## Frontend and API Changes

- Public API now exposes safe per-subject upcoming assignment sessions, without internal version IDs.
- Onboarding and Settings accept global year/series plus per-subject overrides.
- Existing memberships display/retain stored session metadata and pins.
- API reference reads select member pin or non-member DEFAULT.
- Topic progress, task-topic, and attempt-component writes validate the caller's pin.
- New assignment returns safe validation/conflict responses for missing, unavailable, or ambiguous reference context.
- Generated OpenAPI/Zod/React contracts include assignment session and intended session structures.
- No user-facing syllabus version selector or repin control was added.

## Testing and Deployment Changes

- API suite grew from 119 tests in the previous checkpoint to 146 in the Phase 6 closeout/current serial verification.
- Frontend closeout evidence grew from 208 to 227 tests; the current Windows-host rerun hung and was terminated rather than reported as a pass.
- Syllabus/reference suite is 41/41; harness safety/integrity suite is 21/21.
- Migration drift enforcement validates sequential journal entries, hashes, and head.
- Disposable HTTP/auth/RLS evidence records 45/45 and loopback safety 11/11.
- GitHub PR CI now has non-database quality and destructive-authorized disposable database jobs without Production secrets.
- Phase 6 closeout records automatic Vercel deployment and Production smoke PASS at source commit `95ba43f`. Current commit `707e979` differs only by the final closeout report.

## Removed / Replaced / Legacy Behaviour

| Earlier behavior | Current disposition |
| --- | --- |
| Offline validation requiring database pool shutdown | REPLACED; validation/dry-run do not load the importer. |
| `source_file` as version natural key | LEGACY / REPLACED by explicit logical revision identity. |
| Updating a published graph in place | LEGACY / REJECTED; changed content requires a new draft/key. |
| Re-saving selected subjects could repin retained rows | REPLACED by pin preservation. |
| DEFAULT/current used for all membership assignment | REPLACED by strict session applicability and series policy. |
| Client-visible or client-selected version identity | NOT IMPLEMENTED by design; server owns assignment. |
| Ambiguous/absent resolver fallback | REJECTED; both states fail closed. |
| Ordinary local Supabase stack as integration authority | SUPERSEDED by the dedicated disposable harness. |
| Feb/Mar automatic assignment | DEFERRED; represented but policy false and omitted from choices. |
| Automatic repin | NOT IMPLEMENTED and explicitly excluded. |
| Applicability README statement that hosted apply is pending | LEGACY / STALE relative to later Phase 6 closeout evidence. |

## Deferred Boundaries Confirmed by Current State

- Owner final signoff: not claimed.
- Authenticated Production membership QA: not rerun in the final closeout; disposable HTTP/RLS and anonymous hosted smoke were used.
- Real successor (`r002`) import/publication: not performed.
- Feb/Mar product enablement: deferred.
- Automatic membership repin: not implemented.
- Already-published DEFAULT promotion: explicit owner admin step.
- Broader task editing, mounted exam-date mutation UI, onboarding draft persistence, cross-device preferences, and external calendar sync remain outside Phase 6.
