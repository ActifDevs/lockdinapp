Checkpoint ID: 2026-07-28_2156
Generated At: 2026-07-28 21:56
Document Type: Changes
Repository Branch: main
Commit SHA: 009634d77964969d9e01fa553867f94244216f35
Scope: Changes since previous checkpoint (2026-07-27_1835)
Status: Repository Snapshot

# Changes Summary

**Previous Checkpoint:** 2026-07-27_1835 (commit 14b2c757ef1a21f3c79f90c6a637293aa214511f)
**Current Checkpoint:** 2026-07-28_2156 (commit 009634d77964969d9e01fa553867f94244216f35)
**Time Elapsed:** ~27 hours

This checkpoint documents significant data infrastructure improvements including database schema expansion, syllabus import pipeline implementation, and past paper architecture atomization.

---

## Significant Changes

### Database Schema Expansion

**Change:** Added syllabus versioning, assessment component atomization, and learning outcome normalization  
**Previous State:** Basic schema with subjects, syllabus_units, syllabus_topics, tasks, past_papers, exam_dates  
**Current State:** Expanded schema with 4 new tables (syllabus_versions, assessment_components, syllabus_learning_outcomes, learning_outcome_components) and renamed past_papers to past_paper_attempts  
**Reason:** Support syllabus versioning, atomic paper identity, and normalized learning outcomes to enable proper data management and future multi-tenancy  
**Files / Areas Affected:** lib/db/src/schema/* (new files: syllabusVersions.ts, assessmentComponents.ts, syllabusLearningOutcomes.ts, learningOutcomeComponents.ts; renamed: pastPaperAttempts.ts)  
**Database Impact:** Major schema expansion; migration file created; requires cleanup script before execution  
**User-Facing Impact:** Enables proper syllabus versioning and component-level past paper tracking (frontend not yet updated)  
**Compatibility Notes:** Migration requires cleanup script to remove placeholder syllabus data first  

**Details:**
- **syllabus_versions table:** Added version tracking for syllabus changes with exam_board, qualification, label, valid_from, valid_to, is_current, source_file, imported_at
- **assessment_components table:** Added atomic paper code decomposition with paper_code, level, component_name, duration_minutes, total_marks, weighting_percent
- **syllabus_learning_outcomes table:** Added normalized learning outcome storage to avoid duplication across papers
- **learning_outcome_components table:** Added junction table for many-to-many relationships between learning outcomes and assessment components
- **past_paper_attempts table:** Renamed from past_papers; removed paper_code column; added component_id, variant, session columns with check constraints
- **syllabus_units table:** Added syllabus_version_id foreign key for version layer
- **syllabus_topics table:** Added unique constraint on (unit_id, title) for idempotent upserts
- **subjects table:** Added unique constraint on code for data integrity

---

### Database Migration Implementation

**Change:** Created first migration file for schema evolution  
**Previous State:** No migration files; schema managed via drizzle-kit push only  
**Current State:** Migration file 0000_syllabus_reference_and_paper_attempts.sql with hand-edited safe schema evolution  
**Reason:** Establish proper migration workflow for production deployment; enable safe schema evolution without data loss  
**Files / Areas Affected:** lib/db/migrations/0000_syllabus_reference_and_paper_attempts.sql  
**Database Impact:** Adds new tables, renames table, adds constraints; requires cleanup script prerequisite  
**User-Facing Impact:** Enables production database deployment with proper migration history  
**Compatibility Notes:** Migration must be applied after cleanup script removes placeholder syllabus data  

**Details:**
- Hand-edited migration (not auto-generated) to ensure safe evolution from existing schema
- Adds unique constraint on subjects.code
- Creates 4 new tables: syllabus_versions, assessment_components, syllabus_learning_outcomes, learning_outcome_components
- Renames past_papers to past_paper_attempts (preserves any existing data)
- Adds syllabus_version_id column to syllabus_units (NOT NULL, requires cleanup first)
- Adds unique constraints for idempotent upserts
- Adds check constraints for data validation (variant range, score validation, session enum)
- Foreign key constraints with appropriate cascade/set null behavior

---

### Syllabus Import Pipeline Implementation

**Change:** Complete TypeScript import pipeline for CSV syllabus data  
**Previous State:** No import pipeline; raw CSV files existed but no mechanism to load them  
**Current State:** Full import pipeline with validation, normalization, and transactional upsert  
**Reason:** Enable automated loading of syllabus data with idempotency, validation, and error handling  
**Files / Areas Affected:** scripts/src/syllabus/* (cli.ts, parse-csv.ts, normalize.ts, db-upsert.ts, manifest.ts, types.ts, cleanup-placeholder-syllabus.ts, __tests__/)  
**Database Impact:** Enables loading of all 9 subject syllabi with transactional safety  
**User-Facing Impact:** Makes syllabus data available to application (awaiting database deployment)  
**Compatibility Notes:** Requires database deployment and migration execution before use  

**Details:**
- **cli.ts:** CLI interface with validate, import, and dry-run modes; supports file filtering
- **parse-csv.ts:** CSV parsing with header validation, data type validation, error reporting
- **normalize.ts:** Data normalization with hierarchical structure construction, component/variant extraction, learning outcome normalization
- **db-upsert.ts:** Transactional upsert with bulk operations, natural key-based idempotency, progress preservation
- **manifest.ts:** Import configuration with subject metadata and CSV file mapping
- **types.ts:** TypeScript type definitions for normalized data structures
- **cleanup-placeholder-syllabus.ts:** Cleanup script to remove placeholder data before migration
- **Unit tests:** Test coverage for parse-csv, normalize, and db-upsert modules

**Import Pipeline Features:**
- Transactional import per subject with automatic rollback on failure
- Idempotent upsert using natural keys (re-running is safe)
- Bulk operations for performance (avoids per-row round-trips)
- Progress preservation (syllabus_topics.status and notes never overwritten)
- Detailed count reporting (created/updated/unchanged per table)
- Comprehensive error handling with row/column context
- Support for syllabus-wide outcomes (nullable component_id)
- Level preservation in junction table for component-less outcomes

---

### Past Paper Architecture Atomization

**Change:** Atomic paper identity structure replacing combined paper codes  
**Previous State:** Paper codes stored as combined strings (e.g., "9702/42") in past_papers.paper_code  
**Current State:** Atomic structure with component_id (FK to assessment_components), variant (integer 1-5), session (enum)  
**Reason:** Enable querying by component or variant independently; support syllabus versioning; improve data integrity  
**Files / Areas Affected:** lib/db/src/schema/pastPaperAttempts.ts, lib/db/migrations/0000_syllabus_reference_and_paper_attempts.sql  
**Database Impact:** Table rename, column changes, added constraints; display code generated at read time  
**User-Facing Impact:** Enables component-level analysis and variant tracking (frontend not yet updated)  
**Compatibility Notes:** Frontend may still expect combined paper codes; requires UI updates  

**Details:**
- Removed paper_code column from past_paper_attempts
- Added component_id foreign key to assessment_components
- Added variant column (integer 1-5) with check constraint
- Added session column (enum: 'May/June', 'Oct/Nov', 'Feb/Mar', 'Specimen')
- Added check constraints: variant range (1-5), score >= 0, total_marks > 0, score <= total_marks
- Display code "9702/42" generated at read time from subject.code + assessment_components.paper_code + variant
- Assessment components table stores atomic paper codes (e.g., "42" not "9702/42")
- Level-aware component storage (same paper code can appear at AS and A2 levels with different weighting)

---

### Syllabus Versioning Implementation

**Change:** Added syllabus version tracking to support multiple syllabus versions per subject  
**Previous State:** No version tracking; syllabus data directly linked to subjects  
**Current State:** syllabus_versions table with version metadata; all syllabus content linked to version not subject  
**Reason:** Enable syllabus updates without silently reshaping existing user progress; support historical syllabus comparison  
**Files / Areas Affected:** lib/db/src/schema/syllabusVersions.ts, lib/db/src/schema/syllabusUnits.ts, lib/db/migrations/0000_syllabus_reference_and_paper_attempts.sql  
**Database Impact:** New table; foreign key changes; all syllabus content now versioned  
**User-Facing Impact:** Enables syllabus updates without breaking existing progress (awaiting multi-tenancy)  
**Compatibility Notes:** Import pipeline uses source_file as natural key for idempotency  

**Details:**
- syllabus_versions table with exam_board, qualification, label, valid_from, valid_to, is_current, source_file, imported_at
- syllabus_units now linked to syllabus_version_id instead of directly to subject
- Unique constraint on (subject_id, source_file) for idempotency
- Import pipeline creates new version when source_file changes
- Multiple versions per subject supported
- is_current flag for identifying active version
- Version metadata preserved for audit trail

---

### Learning Outcome Normalization

**Change:** Normalized learning outcome storage to avoid duplication  
**Previous State:** Learning outcomes would be duplicated across papers if CSV repeated same outcome  
**Current State:** Normalized storage with junction table for many-to-many relationships  
**Reason:** Avoid data duplication; enable efficient querying; support syllabus-wide outcomes  
**Files / Areas Affected:** lib/db/src/schema/syllabusLearningOutcomes.ts, lib/db/src/schema/learningOutcomeComponents.ts, lib/db/migrations/0000_syllabus_reference_and_paper_attempts.sql  
**Database Impact:** Two new tables; normalized data structure; junction table for relationships  
**User-Facing Impact:** Enables efficient learning outcome queries and component-level outcome mapping  
**Compatibility Notes:** Import pipeline handles normalization automatically  

**Details:**
- syllabus_learning_outcomes table with topic_id, outcome, order_index
- Unique constraint on (topic_id, outcome) prevents duplication
- learning_outcome_components junction table for many-to-many relationships
- Junction table links learning outcomes to assessment_components
- component_id nullable to support syllabus-wide outcomes (e.g., "Mathematical requirements")
- Level preserved in junction table when component_id is null
- Import pipeline recomputes junction table on each import for idempotency

---

### Import Pipeline Testing

**Change:** Added unit tests for import pipeline components  
**Previous State:** No test coverage for import pipeline  
**Current State:** Unit tests for parse-csv, normalize, and db-upsert modules  
**Reason:** Ensure import pipeline correctness; enable regression testing  
**Files / Areas Affected:** scripts/src/syllabus/__tests__/ (parse-csv.test.ts, normalize.test.ts, db-upsert.test.ts)  
**Database Impact:** None (tests use test database)  
**User-Facing Impact:** Improved reliability of import pipeline  
**Compatibility Notes:** Tests require test database configuration  

**Details:**
- parse-csv.test.ts: Tests CSV parsing, validation, error handling
- normalize.test.ts: Tests data normalization, hierarchical structure construction
- db-upsert.test.ts: Tests database upsert logic, idempotency, transaction handling
- Test commands added to scripts/package.json (test, test:unit, test:db)
- Vitest configuration for test execution

---

### Cleanup Script Implementation

**Change:** Added cleanup script to remove placeholder syllabus data  
**Previous State:** No mechanism to remove placeholder data  
**Current State:** cleanup-placeholder-syllabus.ts script to remove test-only "foundations" units/topics  
**Reason:** Migration requires empty syllabus_units to add NOT NULL syllabus_version_id column  
**Files / Areas Affected:** scripts/src/syllabus/cleanup-placeholder-syllabus.ts  
**Database Impact:** Removes placeholder data; prerequisite for migration execution  
**User-Facing Impact:** Enables safe migration execution  
**Compatibility Notes:** Must be run before migration 0000_syllabus_reference_and_paper_attempts.sql  

**Details:**
- Removes test-only "foundations" units and topics
- Safe deletion with verification
- Prerequisite for migration execution
- Command: pnpm --filter @workspace/scripts syllabus:cleanup-placeholders

---

## Database Changes

### Tables Added
- **syllabus_versions** - Version tracking for syllabus changes
- **assessment_components** - Atomic paper code decomposition
- **syllabus_learning_outcomes** - Normalized learning outcome storage
- **learning_outcome_components** - Junction table for LO-component relationships

### Tables Renamed
- **past_papers** → **past_paper_attempts** - Renamed for clarity and to reflect atomic structure

### Columns Added
**syllabus_units:**
- syllabus_version_id (integer, NOT NULL, FK to syllabus_versions)

**syllabus_topics:**
- No new columns, but unique constraint added on (unit_id, title)

**subjects:**
- No new columns, but unique constraint added on code

**past_paper_attempts (renamed from past_papers):**
- component_id (integer, FK to assessment_components, nullable)
- variant (integer, nullable, check constraint 1-5)
- session (enum: 'May/June', 'Oct/Nov', 'Feb/Mar', 'Specimen', NOT NULL)

**Columns Removed:**
- past_paper_attempts.paper_code (replaced by component_id + variant)

### Constraints Added
**Unique Constraints:**
- subjects.code
- syllabus_versions (subject_id, source_file)
- assessment_components (syllabus_version_id, paper_code, level)
- syllabus_units (syllabus_version_id, title)
- syllabus_topics (unit_id, title)
- syllabus_learning_outcomes (topic_id, outcome)
- learning_outcome_components (learning_outcome_id, component_id, level)

**Check Constraints:**
- past_paper_attempts.variant range (1-5)
- past_paper_attempts.score >= 0
- past_paper_attempts.total_marks > 0
- past_paper_attempts.score <= total_marks
- past_paper_attempts.session in ('May/June', 'Oct/Nov', 'Feb/Mar', 'Specimen')

**Foreign Keys:**
- syllabus_versions.subject_id → subjects.id (CASCADE)
- assessment_components.syllabus_version_id → syllabus_versions.id (CASCADE)
- syllabus_units.syllabus_version_id → syllabus_versions.id (CASCADE)
- syllabus_learning_outcomes.topic_id → syllabus_topics.id (CASCADE)
- learning_outcome_components.learning_outcome_id → syllabus_learning_outcomes.id (CASCADE)
- learning_outcome_components.component_id → assessment_components.id (CASCADE)
- past_paper_attempts.component_id → assessment_components.id (SET NULL)

### Migrations
- **Migration 0000_syllabus_reference_and_paper_attempts.sql:** Hand-edited migration for safe schema evolution
- **Status:** Migration file created but not executed
- **Prerequisite:** Cleanup script must remove placeholder syllabus data first

### RLS Changes
- **None:** RLS still not implemented

### Data Migrations
- **None:** No data migrations (database not deployed)

---

## Data Pipeline Changes

### Import Pipeline Implementation
**New Components:**
- scripts/src/syllabus/cli.ts - CLI interface
- scripts/src/syllabus/parse-csv.ts - CSV parsing and validation
- scripts/src/syllabus/normalize.ts - Data normalization
- scripts/src/syllabus/db-upsert.ts - Transactional database upsert
- scripts/src/syllabus/manifest.ts - Import manifest
- scripts/src/syllabus/types.ts - Type definitions
- scripts/src/syllabus/cleanup-placeholder-syllabus.ts - Cleanup script
- scripts/src/syllabus/__tests__/ - Unit tests

**New Commands:**
- pnpm --filter @workspace/scripts syllabus:validate
- pnpm --filter @workspace/scripts syllabus:import
- pnpm --filter @workspace/scripts syllabus:import --dry-run
- pnpm --filter @workspace/scripts syllabus:cleanup-placeholders
- pnpm --filter @workspace/scripts test
- pnpm --filter @workspace/scripts test:unit
- pnpm --filter @workspace/scripts test:db

**CSV Files:**
- No changes to CSV files (still 9 validated files)
- Audit report unchanged

**Normalization:**
- Implemented hierarchical structure construction
- Implemented component/variant extraction
- Implemented learning outcome normalization
- Implemented order index assignment

**Components/Variants:**
- Implemented atomic paper code decomposition
- Assessment components table stores atomic paper codes
- Variant stored as integer 1-5
- Session stored as enum

**Subject Data:**
- No changes to subject data
- Import manifest defines subject metadata
- 9 subjects ready for import

---

## Frontend Changes

**No significant frontend changes** since previous checkpoint.

**Minor Changes:**
- Cursor audit prompt added (docs/cursor_audit_prompt_v2.md)
- Syllabus page UI improvements (commit d0b61f4)
- Landing page hero mobile view fix (commit e2c9160)
- Thick solid borders removed (commit c24efc3)

**Status:**
- Frontend still uses mock localStorage authentication
- Frontend still uses hardcoded subject catalog
- Frontend not yet updated to use new component/variant structure for past papers
- Frontend not yet connected to real database

---

## Removed / Replaced / Legacy Behaviour

### Removed Functionality

**Placeholder Syllabus Data:**
- Cleanup script removes test-only "foundations" units/topics
- Reason: Required for migration to add NOT NULL syllabus_version_id column
- Status: Script implemented but not yet executed

### Replaced Functionality

**Past Paper Storage:**
- **Previous:** Combined paper code stored as string (e.g., "9702/42")
- **Current:** Atomic structure with component_id, variant, session
- **Reason:** Enable component-level analysis and variant tracking
- **Status:** Schema changed; frontend not yet updated

**Table Name:**
- **Previous:** past_papers
- **Current:** past_paper_attempts
- **Reason:** More descriptive name reflecting atomic structure
- **Status:** Renamed via migration; data preserved

### Legacy Behaviour

**Mock Authentication:**
- Still mock (localStorage)
- Still must be replaced before production
- No changes since previous checkpoint

**Single-User Architecture:**
- Still single-user (no user_id columns)
- Still must be redesigned for multi-tenancy
- No changes since previous checkpoint

**No Database Deployment:**
- Schema expanded but still not deployed
- Migration ready but not executed
- Import pipeline ready but not used

### Obsolete Code

**Mockup Sandbox:**
- artifacts/mockup-sandbox directory exists but not actively used
- May contain experimental or deprecated code
- No changes since previous checkpoint

---

## Technical Debt

### Authentication
- **Debt:** Mock localStorage authentication (unchanged)
- **Impact:** Security vulnerability; no real user management
- **Required:** Implement proper authentication backend

### Multi-tenancy
- **Debt:** No user_id columns in schema (unchanged)
- **Impact:** Cannot support multiple users; data isolation impossible
- **Required:** Add user ownership to all user-owned tables

### Database Deployment
- **Debt:** Schema expanded but not deployed (NEW: schema now ready)
- **Impact:** Application cannot function with real data
- **Required:** Deploy database and run migration

### Data Import
- **Debt:** No CSV import pipeline (RESOLVED: pipeline now implemented)
- **Impact:** Reference data inaccessible to application (RESOLVED: pipeline ready)
- **Required:** Execute import pipeline after database deployment

### Frontend-Backend Alignment
- **Debt:** Frontend not updated for new schema (NEW)
- **Impact:** Frontend may not use component/variant structure
- **Required:** Update frontend to use atomic paper identity

### Security
- **Debt:** No authentication middleware on API routes (unchanged)
- **Impact:** All endpoints publicly accessible
- **Required:** Add authentication and authorization middleware

### Testing
- **Debt:** No E2E tests (unchanged)
- **Impact:** No end-to-end validation of full application
- **Required:** Implement E2E testing

---

## Migration Notes

### For Future AI Agents

**Critical Architectural Decisions:**
1. **Monorepo Structure:** pnpm workspace with shared libraries - maintain this pattern
2. **Schema-First:** Database schema drives types - modify schema first, then regenerate
3. **OpenAPI Contract:** API spec is source of truth - regenerate clients after changes
4. **Subject Catalog:** Keep frontend catalog in sync with available CSV files
5. **Color Scheme:** Maintain subject accent colors for consistency
6. **Import Pipeline:** Use scripts/src/syllabus/cli.ts for all syllabus operations
7. **Syllabus Versioning:** All syllabus content linked to version, not subject directly
8. **Atomic Paper Identity:** Use component/variant/session structure, not combined paper codes

**Files to Modify Carefully:**
- lib/db/src/schema/* - Changes require migration planning
- lib/db/migrations/* - Migration files are hand-edited for safety
- lib/api-spec/openapi.yaml - Changes require client regeneration
- data/syllabi/raw/*.csv - Validated data; modifications require re-validation
- pnpm-workspace.yaml - Security settings; maintain minimum release age
- scripts/src/syllabus/manifest.ts - Import manifest; defines subject metadata

**Files That Should Not Be Modified:**
- lib/api-zod/src/generated/* - Auto-generated; do not edit manually
- lib/api-client-react/src/generated/* - Auto-generated; do not edit manually
- data/syllabi/raw/_audit_report.json - Audit report; do not modify manually
- lib/db/migrations/0000_syllabus_reference_and_paper_attempts.sql - Hand-edited; modify with extreme caution

**Known Limitations to Address:**
- Authentication must be replaced before production use
- Database must be deployed and migration executed
- Cleanup script must be run before migration
- Import pipeline must be executed after migration
- Multi-tenancy must be implemented for multiple users
- Security middleware must be added to all API routes
- Frontend must be updated to use component/variant structure

**Development Workflow:**
1. Modify schema in lib/db/src/schema/
2. Generate migration: pnpm --filter @workspace/db generate
3. Review migration file (may need hand-editing for safety)
4. Update OpenAPI spec in lib/api-spec/openapi.yaml
5. Regenerate clients: Orval will auto-generate
6. Update frontend to use new types
7. Test changes locally
8. For syllabus changes: use import pipeline, never manual database manipulation

**Import Pipeline Workflow:**
1. Validate CSV: pnpm --filter @workspace/scripts syllabus:validate
2. Dry run import: pnpm --filter @workspace/scripts syllabus:import --dry-run
3. Execute import: pnpm --filter @workspace/scripts syllabus:import
4. Verify counts in output
5. Check database with Drizzle Studio if needed

**Migration Prerequisites:**
1. Run cleanup script: pnpm --filter @workspace/scripts syllabus:cleanup-placeholders
2. Verify placeholder data removed
3. Apply migration: pnpm --filter @workspace/db migrate
4. Verify migration success
5. Execute import pipeline

---

## Compatibility Notes

### Breaking Changes
**Database Schema:**
- past_papers table renamed to past_paper_attempts
- paper_code column removed from past_paper_attempts
- component_id, variant, session columns added to past_paper_attempts
- syllabus_units.syllabus_version_id added as NOT NULL (requires cleanup first)

**Impact:**
- Frontend code referencing past_papers must be updated
- Frontend code using paper_code must be updated to use component/variant
- Migration requires cleanup script prerequisite

### Non-Breaking Changes
- All other changes are additive or internal
- Import pipeline is new functionality
- Syllabus versioning is new functionality
- Learning outcome normalization is new functionality

### Upgrade Path
**For Database:**
1. Run cleanup script to remove placeholder syllabus data
2. Apply migration 0000_syllabus_reference_and_paper_attempts.sql
3. Execute import pipeline to load syllabus data
4. Update frontend to use new schema

**For Frontend:**
1. Update API client references (past_papers → past_paper_attempts)
2. Update past paper forms to use component/variant/session
3. Update past paper display to generate paper codes from atomic components
4. Test all past paper functionality

---

## Summary

This checkpoint represents a major data infrastructure milestone. The database schema has been significantly expanded with syllabus versioning, assessment component atomization, and learning outcome normalization. A complete import pipeline has been implemented with validation, normalization, and transactional upsert capabilities. The past paper architecture has been atomized to support component-level analysis and variant tracking.

The project has moved from having no data import capability to having a production-ready import pipeline with comprehensive safety mechanisms. The database schema now supports proper syllabus versioning and atomic paper identity, laying the groundwork for future multi-tenancy and advanced analytics.

However, critical blockers remain: no production database deployment, no real authentication, no multi-tenancy support, and the frontend not yet updated to use the new schema. The next major phase should focus on database deployment, import execution, and frontend-backend alignment.
