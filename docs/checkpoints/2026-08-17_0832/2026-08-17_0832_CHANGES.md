Checkpoint ID: 2026-08-17_0832
Generated At: 2026-08-17 08:32:08 UTC
Repository Branch: phase3-multitenancy
Snapshot Source Commit: 0f34c77be354fa6b1db11dcc1e1c1c1ad37caa8d
Scope: Phase 3 Multi-Tenancy Final Closeout
Status: PHASE 3 COMPLETE ON INTEGRATION BRANCH — MAIN MERGE PENDING

# Phase 3 Changes

## Chronology

| Stage | Outcome |
| --- | --- |
| Stop 1 | Architecture and migration audit completed; personal/shared boundaries and migration sequence established |
| Slice 1 | Durable caller-owned `user_subjects`, transactional replacement, onboarding membership/starter-task integration |
| Slice 2A | Caller-owned `topic_progress`, trusted upsert/reset RPCs, caller overlay on shared topics |
| Slice 2B | Removed legacy shared progress fields from `syllabus_topics` |
| Slice 3 | Caller-owned `past_paper_attempts`, required four-digit `year`, isolated performance aggregation |
| Slice 4 | Caller-owned `exam_dates`, isolated chronological/Dashboard reads, no update surface |
| Slice 5 | API, OpenAPI/generated clients, frontend consumers, user-scoped browser storage, and cache/auth reconciliation |

Final browser QA passed. The final hosted whole-Phase-3 two-user E2E retry passed every application assertion, cleanup passed, and the persistent hosted baseline was restored with no disposable residue. Slice 5 integration passed. The main merge simulation passed after the understood `subjects.ts` resolution was applied and verified.

## Phase 3 Migrations

### `0004_colossal_pixie.sql`

- Creates `user_subjects` with `(user_id, subject_id)` primary key and a subject/version integrity foreign key.
- Adds caller-owned RLS, initial table grants, timestamps, and a five-membership limit trigger.
- Adds `lockdin_replace_user_subjects(integer[])` for atomic 1–5 unique valid subject replacement.
- Replaces Phase 2 onboarding so profile completion, memberships, and starter tasks occur atomically for `auth.uid()`.

### `0005_restrict_user_subject_writes.sql`

- Removes direct authenticated INSERT/UPDATE/DELETE policies and grants from `user_subjects`.
- Keeps authenticated SELECT only; all membership mutations must use the trusted onboarding/replacement RPC boundary.

### `0006_slippery_squirrel_girl.sql`

- Creates caller-owned `topic_progress` keyed by `(user_id, topic_id)` with status and note constraints.
- Enables own-row SELECT RLS and denies direct authenticated table writes.
- Adds trusted caller-derived upsert and reset RPCs; neutral default/no-note state removes the personal row.

### `0007_eager_squadron_supreme.sql`

- Removes the deprecated shared `status` and `notes` columns from `syllabus_topics`.
- Leaves `topic_progress`, its policies, grants, and RPCs unchanged; personal progress no longer exists on shared topic rows.

### `0008_uneven_mojo.sql`

- Fails closed if `past_paper_attempts` is non-empty rather than inventing ownership or year.
- Adds required `user_id` and required four-digit `year`, Auth cascade, caller-oriented indexes, own-row SELECT/INSERT/DELETE RLS, and restricted grants.
- Does not add an update surface; percentage remains derived by application/database logic from marks.

### `0009_dear_mathemanic.sql`

- Locks and fails closed if `exam_dates` is non-empty rather than inventing owners.
- Adds required `user_id`, Auth cascade, chronological caller index, own-row SELECT/INSERT/DELETE RLS, and restricted grants.
- Intentionally provides no authenticated UPDATE privilege.

Migration journal reconciliation confirmed exactly `0000`–`0009`; migration `0010` is absent.

## Final Reconciliation

- API/OpenAPI/generated-client contracts agree on all Phase 3 domains.
- Ownership aliases are rejected instead of trusted.
- Auth identity transitions clear personal query state.
- Personal browser-storage keys are user-qualified where required.
- Dashboard and Progress aggregate only caller-owned rows and current memberships.
- Shared catalogue rows remain reference-only.
- QA fixture cleanup: **PASS**
- Hosted persistent baseline restored: **PASS**
- Main merge simulation: **PASS** after the proven `subjects.ts` combined resolution.

## Release State

Phase 3 is complete on `phase3-multitenancy`. It is not yet merged to `main`, deployed to managed full-stack Production, or followed by Phase 4 work.
