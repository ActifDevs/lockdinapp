Checkpoint ID: 2026-09-01_2245
Generated At: 2026-09-01 22:45
Document Type: Changes
Repository Branch: main
Commit SHA: 0d2963c854ad3fbf9ce0e111c695138120fd42e5
Scope: Material changes since checkpoint 2026-08-30_1339 at commit 707e979
Status: Repository Snapshot

# Changes Since Previous Checkpoint

Previous checkpoint: `2026-08-30_1339` at `707e9793506fe5707d1cd836f500b8d4bbc0c990` (Phase 6 closed).

Current baseline: `0d2963c854ad3fbf9ce0e111c695138120fd42e5`.

This is a Phase 7 readiness and pre-beta checkpoint, not a final Phase 7 closeout. The current range adds product analytics, error monitoring, hosted Auth hardening evidence, isolated restore proof, and invite-only beta UX/materials. It contains no application-schema migration or syllabus CSV change.

## Significant Changes

### 1. Phase 7 scope and owner gates established

```text
Change:                Phase 7 was reconciled into explicit beta, analytics, monitoring, auth, restore, and closeout slices.
Previous State:         Phase 6 was closed; Phase 7 implementation decisions were not yet recorded.
Current State:          Beta target (8–12 active users / 10–14 days), PostHog EU, Sentry, invite-only posture, and restore approach are documented; Phase 7 remains open.
Reason:                 Reports 113–114 record repository reconciliation and owner decisions.
Files / Areas Affected: docs/cursor/reports/113*, 114*.
Database Impact:        None.
User-Facing Impact:     None directly; establishes the controlled-beta boundary.
Compatibility Notes:    Later closeouts supersede early planning rows; do not treat Report 114's “post-Phase-7” label as proof that remaining pre-beta owner gates are optional.
```

### 2. Server-side PostHog product analytics implemented

```text
Change:                Added privacy-conscious product telemetry and a first-party account-created endpoint.
Previous State:         No product telemetry SDK or event capture existed.
Current State:          posthog-node captures account_created, onboarding_completed, task_created, and past_paper_attempt_created with allow-listed properties and one HMAC alias per authenticated user.
Reason:                 Owner-approved minimal activation/usage evidence for the controlled beta.
Files / Areas Affected: artifacts/api-server/src/lib/analytics/, analytics route, profile/tasks/past-paper routes, frontend pending-signup handoff, OpenAPI/generated clients, .env.example, privacy page, package/lock files.
Database Impact:        None; no analytics identity/state table and no migration 0016.
User-Facing Impact:     No workflow change; privacy disclosure added.
Compatibility Notes:    The browser PostHog SDK was removed. One EU project separated by mandatory environment supersedes the earlier separate-project design. Analytics failure is a no-op.
```

### 3. Frontend/API Sentry monitoring and private source maps implemented

```text
Change:                Added sanitized error capture, release/environment tags, request-id context, and conditional source-map uploads.
Previous State:         Errors were locally logged/handled without hosted Sentry.
Current State:          React and Express Sentry integrations are present; Preview delivery/symbolication and Production configuration/build uploads are evidenced. Replay, tracing, profiling, feedback, default PII, and deliberate Production error injection remain off.
Reason:                 Owner-approved actionable reliability monitoring for beta.
Files / Areas Affected: frontend/API monitoring modules, route error boundary, central API error handler, build scripts, Vite config, .env.example, privacy page, package/lock files.
Database Impact:        None.
User-Facing Impact:     Route errors can be reported without changing product responses; no Session Replay UI.
Compatibility Notes:    Sanitizers intentionally redact arbitrary exception text. Release must match the Git SHA used for source-map upload.
```

### 4. Vercel OpenTelemetry runtime packaging hotfix

```text
Change:                Promoted required OpenTelemetry packages to direct API/runtime dependencies.
Previous State:         The first Production Sentry deployment built successfully but API functions failed at startup because externalized transitive modules were absent.
Current State:          @opentelemetry/core, instrumentation, and sdk-trace-base are direct dependencies; replacement Production deployment and normal smoke passed.
Reason:                 Vercel's serverless package did not reliably include Sentry's transitive externalized modules.
Files / Areas Affected: artifacts/api-server/package.json, pnpm workspace/lockfile, related package manifests.
Database Impact:        None.
User-Facing Impact:     Restored Production API availability.
Compatibility Notes:    Do not remove these as “unused”; Sentry auto-setup remains disabled, but runtime module resolution still requires them.
```

### 5. Hosted Supabase Auth hardened and canonical redirects cleaned

```text
Change:                Corrected the hosted Site URL, added exact current redirects, raised hosted minimum password length, verified flows, and removed six obsolete redirects.
Previous State:         Site URL pointed at a legacy Vercel host; hosted minimum password length was 6; historical redirect entries remained.
Current State:          Site URL is lockdinapp-web.vercel.app; only /auth/callback and /update-password redirects remain; hosted minimum is 8; email confirmation and login work; OAuth/anonymous/CAPTCHA are off.
Reason:                 Phase 7 provider-layer auth abuse/redirect review.
Files / Areas Affected: Hosted Supabase Auth configuration and Reports 117/118; no repository config change for the hosted values.
Database Impact:        None; migration head remains 0015.
User-Facing Impact:     Login/recovery uses the canonical host; stronger minimum for newly set passwords.
Compatibility Notes:    Local supabase/config.toml still uses minimum 6 and is not hosted truth. Later Report 118 turns public self-signup off, superseding Report 117's earlier enabled state.
```

### 6. Public signup disabled and controlled invitation path proven

```text
Change:                Hosted public self-signup was disabled and an owner-controlled Dashboard invitation path was exercised.
Previous State:         Email self-signup was enabled after Slice 7.4; public /signup still offered registration.
Current State:          General public signup is off; email/password and confirmation remain on; a controlled QA invitation reached onboarding/dashboard and was cleaned up safely.
Reason:                 Reduce beta access to authorized participants without introducing CAPTCHA friction.
Files / Areas Affected: Hosted Supabase Auth configuration; Report 118.
Database Impact:        No schema change. Controlled QA user-owned rows were safely cleaned up per report evidence.
User-Facing Impact:     Only invited accounts can enter the beta.
Compatibility Notes:    AuthProvider.signup remains in code but is not a viable public path while hosted signup is disabled.
```

### 7. Isolated logical restore proof completed

```text
Change:                Restored Production schema/shared reference data into a temporary isolated Supabase project and verified fidelity.
Previous State:         Backup/restore strategy was approved but not execution-proven.
Current State:          16/16 migration hashes/timestamps, catalogue aggregates, RLS/policies, and custom Auth trigger matched; Production users/study rows were excluded; Production stayed unchanged; temporary target and artifacts were deleted.
Reason:                 Phase 7 beta-readiness recovery proof.
Files / Areas Affected: docs/cursor/reports/118* only; temporary SQL was intentionally not committed.
Database Impact:        Temporary isolated target only; Production was source-only and unchanged.
User-Facing Impact:     None.
Compatibility Notes:    roles.sql was not applied after a managed-host permission rejection; default hosted roles were sufficient. Do not rerun against Production or restore user PII merely to repeat proof.
```

### 8. Invitation-only signup UX and beta materials added

```text
Change:                Replaced the public signup form with controlled-beta invitation copy; updated landing/login language; added deletion contact and beta materials.
Previous State:         Hosted signup was off, but /signup still appeared to offer normal registration and Report 118 contradicted itself about timing.
Current State:          /signup contains no name/email/password/Google form, explains the invitation email flow, and links to sign-in/privacy. Landing CTAs say Invitation only. Beta welcome/support/feedback/deletion drafts exist.
Reason:                 Owner classified truthful signup messaging as pre-beta work.
Files / Areas Affected: signup.tsx, login.tsx, index.tsx, document-title.tsx, privacy.tsx, auth-pages.test.ts, docs/beta/, Report 119.
Database Impact:        None.
User-Facing Impact:     Visitors no longer submit a form that hosted Auth will reject; deletion/privacy contact is visible.
Compatibility Notes:    Recovery/callback/update-password structure is unchanged. Beta materials are drafts; they do not mean invitations were sent or beta started.
```

## Database Changes

No Drizzle schema or migration changed between the previous checkpoint baseline and this checkpoint:

- Repository migrations remain 16 (`0000`–`0015`).
- Head remains `0015_silent_sentinel`.
- `0016` is absent.
- No tables, columns, indexes, constraints, RLS policies, or application functions were added in Phase 7.
- Hosted Auth configuration changed, but that is provider configuration, not database migration authority.
- Restore used a temporary project and did not alter Production.

The checkpoint verified migration integrity locally. Report 118 provides current hosted hash/timestamp correspondence and structural restore evidence.

## Data Pipeline Changes

- No syllabus CSV, semantic audit, manifest, normalized graph code, or applicability manifest changed since the previous checkpoint.
- The applicability README was corrected after the previous baseline to say hosted population is complete; this reconciles an older stale statement.
- No subject was added or removed. Information Technology remains absent.
- No real successor revision was created, adopted, or published.
- No series/variant/session model changed. Feb/Mar auto-assignment remains deferred.
- Restore proof independently reproduced shared catalogue counts without copying Production user rows.

## Frontend Changes

- Added Sentry initialization, route error boundary, sanitization, environment/release helpers, and source-map support.
- Added local pending-signup analytics handoff; no browser PostHog SDK.
- Replaced public signup with invitation-only state and updated landing/login/document titles.
- Expanded privacy copy for PostHog, Sentry, and account-deletion requests.
- Added beta operational materials under `docs/beta/`.

Current discrepancy: the privacy page still says Preview and Production use separate PostHog projects, while Report 115's later approved topology uses one project separated by `environment`. It also says hosted Sentry capture is unproven, while Report 116 later proves Preview capture/symbolication and Production configuration/uploads. These are documentation/UI copy defects, not architecture changes, and were not fixed in this checkpoint.

## Backend / API Changes

- Added analytics contract, HMAC aliasing, PostHog client, and authenticated empty-body `/analytics/account-created` endpoint.
- Added product-event calls after successful onboarding, task creation, and paper-attempt creation.
- Added Sentry initialization/sanitization and central error-handler capture with request ID.
- Added build-time API source-map upload and the OpenTelemetry runtime packaging fix.
- API business endpoints, strict assignment, RLS model, and database schema were otherwise unchanged.

## Testing / Operational Evidence Changes

Compared with the prior Phase 6 checkpoint, tracked evidence now includes:

- PostHog Preview/Production configuration and observed approved events.
- Sentry Preview delivery/privacy/runtime tags, hosted symbolication, Production uploads, and normal smoke.
- Hosted Auth configuration, canonical redirect cleanup, login/logout, and recovery delivery/path proof.
- Invite-only account path and disabled public signup proof.
- Isolated restore fidelity and cleanup proof.
- Current invite-only UI focused tests/build proof.

This checkpoint independently passed TypeScript, API 187/187 serially, focused auth 37/37, syllabus 41/41, harness 21/21, migration integrity, and offline CSV validation. The full frontend suite again hung on the Windows host and is not claimed as passed here.

## Removed / Replaced / Legacy Behaviour

| Behaviour | Current disposition |
| --- | --- |
| Browser PostHog / anonymous browser identity | REMOVED before final analytics rollout; server-only unified HMAC identity is authoritative. |
| Separate PostHog Preview/Production projects | REPLACED by one EU project plus mandatory `environment` and separate alias secrets. |
| Legacy Auth Site URL `lockedin-study.vercel.app` | LEGACY / OBSOLETE; replaced by `lockdinapp-web.vercel.app`. |
| Six historical Supabase redirect entries | REMOVED after owner-approved validation. |
| Public signup form | REMOVED from `/signup` for the controlled-beta posture. |
| Hosted public email self-signup | DISABLED after Slice 7.4; invite-only is authoritative. |
| Express auth rate limiter proposal | NOT IMPLEMENTED; Supabase provider-native controls are the correct boundary. |
| CAPTCHA-before-beta requirement | DEFERRED under invite-only signup; evidence-triggered Turnstile remains possible. |
| Production error injection for monitoring proof | DELIBERATELY NOT PERFORMED; Preview plus build/smoke evidence is the accepted boundary. |
| Restore-over-Production concept | REJECTED; temporary isolated target is the proven model. |
| “Phase 7 readiness engineering passed = Phase 7 closed” | INVALID; beta, fixes, and final closeout remain. |

## Current Historical Interpretation

Reports 115–119 must be read using later-evidence precedence:

- Report 115's final one-project analytics topology supersedes early separate-project planning.
- Report 116's final symbolication/Production sections supersede intermediate blocked or pre-remediation Sentry observations.
- Report 117's final redirect cleanup supersedes its earlier six-entry review and earlier deferred recovery request.
- Report 118's public-signup OFF state supersedes Report 117's email self-signup enabled state.
- Report 119 makes invitation-only `/signup` messaging pre-beta and supersedes Report 118's contradictory post-beta suggestion.

No repository evidence proves that the controlled beta started, that Ghana is the final jurisdiction, that minors/guardian process is resolved, that regulator/legal clearance exists, or that Phase 7/public release is closed.
